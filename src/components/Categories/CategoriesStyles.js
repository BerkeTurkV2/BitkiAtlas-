import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        borderWidth: 0.6,
        borderRadius: 16,
        marginVertical: 15,
        marginLeft: 16,
    },
    categorieName: {
        padding: 10,
        fontSize: 12,
    }
});